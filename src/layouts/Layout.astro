---
import '@fontsource-variable/montserrat';
import { ClientRouter } from 'astro:transitions';
import { keywords } from '../data/keywords.js'; // Ajusta la ruta si es necesario
import Background from './Background.astro';
import Footer from './Footer.astro';
import Header from './Header.astro';

interface Props {
	title: string;
	description: string;
}

const { title, description } = Astro.props;
const metaKeywords = keywords.join(', ');
---

<!doctype html>
<html lang="es" class="scroll-smooth">
    <head>
        <ClientRouter />
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <meta name="description" content={description} />
        <meta name="keywords" content={metaKeywords} />
        <meta name="generator" content={Astro.generator} />
        <meta name="geo.region" content="CL-RM" />
        <meta name="geo.placename" content="Santiago de los Caballeros" />
        <meta name="geo.position" content="-19.450, -70.690" />
        <meta name="ICBM" content="-19.450, -70.690" />
        <meta name="business:contact_data:street_address" content="Autopista Duarte Km 4 1/2. Canabacoa" />
        <meta name="business:contact_data:locality" content="Santiago de los Caballeros" />
        <meta name="business:contact_data:country_name" content="República Dominicana" />
        <meta name="business:contact_data:email" content="jmsterminaciondelaconstruccion@hotmail.com" />
        <meta name="business:contact_data:phone_number" content="+180986122588" />
        <meta name="business:contact_data:website" content="https://www.instagram.com/jmsterminaciones/" />
        <link rel="icon" type="image/svg+xml" href="/favicon.ico" />
        <title>{title}</title>
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.0/css/all.min.css">
    </head>
    <body class="bg-main-white flex flex-col min-h-screen">
        <Background />
        <Header />
        <main class="flex-grow flex flex-col">
            <slot />
        </main>
        <Footer />
    </body>
</html>
<style is:global>
    @import url("https://fonts.cdnfonts.com/css/premier-2019");

    :root {
        color-scheme: light;
    }
    html {
        font-family: "Montserrat Variable", sans-serif;
    }
    body {
        overflow-x: hidden;
    }

    h1, h2, h3 {
        font-family: "Premier 2019", sans-serif;
        font-weight: 100;
    }

    /* Responsive typography */
    h1 {
        font-size: clamp(2rem, 5vw, 3.5rem);
    }
    h2 {
        font-size: clamp(1.5rem, 4vw, 2.5rem);
    }
    h3 {
        font-size: clamp(1.25rem, 3vw, 2rem);
    }
    p {
        font-size: clamp(1rem, 2vw, 1.125rem);
    }

    @media (prefers-reduced-motion: reduce) {
        html {
            scroll-behavior: auto;
        }
    }
</style>
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "HardwareStore",
  "name": "JMS Terminación de la Construcción",
  "image": "https://www.instagram.com/jmsterminaciones/profile_pic", 
  "url": "https://www.instagram.com/jmsterminaciones/",
  "telephone": "+180986122588",
  "address": {
    "@type": "PostalAddress",
    "streetAddress": "Autopista Duarte Km 4 1/2. Canabacoa",
    "addressLocality": "Santiago de los Caballeros",
    "addressCountry": "DO"
  },
  "openingHoursSpecification": [
    {
      "@type": "OpeningHoursSpecification",
      "dayOfWeek": [
        "Monday",
        "Tuesday",
        "Wednesday",
        "Thursday",
        "Friday"
      ],
      "opens": "08:00",
      "closes": "18:00"
    },
    {
      "@type": "OpeningHoursSpecification",
      "dayOfWeek": "Saturday",
      "opens": "08:00",
      "closes": "12:00"
    }
  ],
  "sameAs": [
    "https://www.instagram.com/jmsterminaciones/",
    "https://g.co/kgs/mLKUWYn"
  ]
}
</script>
